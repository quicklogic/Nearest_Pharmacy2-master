<?xml version="1.0" encoding="utf-8" ?>
<local:FreshBaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Nearest_Pharmacy.Pages.MenuPage"
             xmlns:local="clr-namespace:FreshMvvm;assembly = Nearest_Pharmacy" Title="Меню" >
    <local:FreshBaseContentPage.Content >
        <StackLayout HorizontalOptions="CenterAndExpand" Padding="50">
                <Button Text ="На главную" Command="{Binding goMain}"/>
                <Button Text ="Профиль" Command="{Binding goUserProfile}"/>
                <Button Text ="Заказы" Command="{Binding goOrders}"/>
                <Button Text ="Корзина" Command="{Binding goBasket}"/>
               

          <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand" VerticalOptions="End">
                <Button Text="Войти" Command="{Binding goLogin}"/>
                <Button Text="Регистрация" Command="{Binding goRegister}"/>
            </StackLayout>
        </StackLayout>
    </local:FreshBaseContentPage.Content>
</local:FreshBaseContentPage>